{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd rag-web-app/backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd rag-web-app/backend && python app.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

